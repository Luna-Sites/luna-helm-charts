apiVersion: v1
kind: Service
metadata:
  name: {{ include "lunasites-frontend.fullname" . }}
  labels:
    {{- include "lunasites-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lunasites-frontend.selectorLabels" . | nindent 4 }}
