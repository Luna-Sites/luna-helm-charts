apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lunasites-backend.fullname" . }}-test-connection"
  labels:
    {{- include "lunasites-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lunasites-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
