apiVersion: v1
kind: Service
metadata:
  name: {{ include "lunasites-backend.fullname" . }}
  labels:
    {{- include "lunasites-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lunasites-backend.selectorLabels" . | nindent 4 }}
